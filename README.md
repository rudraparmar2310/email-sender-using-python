# email-sender-using-python
A simple Python mail-sender with a clean Tkinter GUI that guides the user through entering sender email, app password, and receiver email, validates inputs, lets the user compose the message, performs SMTP authentication, and then sends the email (or shows “not sent” on failure). Tech: Python, Tkinter, smtplib; supports optional file attachment.
